# TokenIQ — dependencies
# Only the HTTP gateway needs these. control_plane.py and demo.py are stdlib-only,
# and control_plane_demo.html runs in any browser with nothing installed.

fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
